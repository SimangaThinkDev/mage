name: Release

on:
  push:
    tags:
      - "v*"

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build .deb
        run: |
          mkdir -p deb/DEBIAN deb/usr/local/bin
          cp main.sh deb/usr/local/bin/mage
          chmod +x deb/usr/local/bin/mage
          cat > deb/DEBIAN/control <<EOF
          Package: mage
          Version: ${GITHUB_REF_NAME#v}
          Section: utils
          Priority: optional
          Architecture: all
          Maintainer: SimangaThinkDev
          Description: Installs any .AppImage with a terminal command and desktop launcher entry
          EOF
          dpkg-deb --build deb mage.deb

      - name: Publish release
        uses: softprops/action-gh-release@v2
        with:
          files: mage.deb
